# BUG-0017: ModuleNotFoundError: No module named 'diskcache'
- Date: 2026-08-24
- Status: Resolved
- Severity: High

## Symptom

WebUI fails at startup with:

```
ModuleNotFoundError: No module named 'diskcache'
```

Error propagation path:
`webui.py` → `initialize.imports()` → `shared_init.py` → `shared.py` → `shared_items.py` → `script_callbacks.py` → `extensions.py` → `cache.py` → `import diskcache`

## Root Cause

A Colab base image update removed `diskcache` from the pre-installed packages.

## Fix

Added `diskcache` to the Requirements cell:

```python
!pip install pyngrok pytorch_lightning kornia open_clip_torch diskcache -qq
```

## Changed Files
- `fast_stable_diffusion_AUTOMATIC1111.ipynb` — Requirements cell

## Prevention

Any package removed from Colab's base image must be explicitly installed in the Requirements cell.
